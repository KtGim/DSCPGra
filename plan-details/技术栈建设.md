
> [!NOTE] 现状
> 目前项目技术栈主要基于 Vue 2.6 + Vue Router 3（history）+ Vue CLI 4 + Vuex 3 + Element UI 2.x + Axios 0.21.0，整体相对落后与目前的新版本

## 一、基于 Axios 0.21.0 的 request 封装
- 目前能力丰富,包含以下功能
	- API `baseURL` 和默认超时。
	- 前端 Cookie Token 读取及请求头注入。
	- GET 参数序列化。
	- 业务状态码判断。
	- 登录失效弹窗和跳转。
	- 全局错误消息。
	- Blob 文件流处理。
	- 可选重复请求取消。
	- 可选延迟返回。
- 问题:
	- 该版本 CancelToken 基于已废弃的议案开发，且本身状态管理存在问题，会导致内存问题，竞态问题。
	- GET 参数序列化。可以直接使用 Axios 转化，无需手动修改，参数格式化问题（null/undefined）等等可以在具体业务代码中解决，或者通过统一封装的 table 组件内聚解决这个问题，还可以通过i辅助函数过渡性解决
	- request 承载过多的内容，应当只需要负责请求相关的逻辑
	- 下载接口返回内容不一致
	- 请求头的 token 字段不一致，目前项目中有三类 token 放在请求投中，另外下载接口还支持直接通过 跳转下载，token 可能会直接暴露
- 后续开发组件化能力，在组件库统一维护 request.js,各个业务点只负责调用
	- 增加接口异常日志上报
	- 完善重复请求取消逻辑，目前的 cancelKey 会重复导致相同请求不同参数会被认为是同一个请求
## 二、vue3 全链路升级
- vue2 框架需要稳步被替代掉，新框架考虑 vue3 学习成本低
- 搭配 vue3 使用新的 pinia 管理全局状态，相较于 vuex 更加便捷和易于维护，vuex4 官方已停止开发，推荐 pinia，ts 友好，不需要嵌套 namespace
	```javascript
		// Vuex 4：store + module + mutation + action
		const store = createStore({
		  state: () => ({ count: 0 }),
		  mutations: { add(s, n) { s.count += n } },
		  actions: { async fetch() { /* 只能这里做异步 */ } },
		})
		// 组件里
		store.commit('add', 1)   // 同步必须走 mutation
		store.dispatch('fetch')  // 异步走 action
		
		// Pinia：直接定义，无 mutation
		export const useCounter = defineStore('counter', {
		  state: () => ({ count: 0 }),
		  actions: {
			add(n) { this.count += n },        // action 里可直接改 state
			async fetch() { /* 同步异步统一 */ },
		  },
		})
		// 组件里
		const store = useCounter()
		store.add(1)   // 直接调用 action，内部同步改 state
	```
- vue router4.x 替换 router3,主要差异如下
	![[Pasted image 20260806090328.png]]
- 路由可以先行升级，官方有配套升级方案
	- 用官方 `vue-router` 4 升级指南 + `@vue/compat` 过渡，一次性改掉 `createRouter` 即可跑通，守卫内 next → return 的改动集中且机械；
	- 之前定制过 `mode: 'hash'` 的 Legacy 子应用，升级时统一为 `createWebHistory`（与基座一致），避免 hash 与 history 混合
## 三、eslint + Prettier（考虑子应用）
- 目前eslint 只有基础规则，不能覆盖整个业务开发过程
- Prettier 没有使用，可以约束代码规范达到编码统一的目的
![[Pasted image 20260805182307.png]]

## 四、Typescript 类型校验

- 目前项目中没有对参数类型，组件props 进行类型规范
- vue3 支持 typescript，可以在开发阶段对不存在的属性进行提前感知报错，提高代码质量
	- 可以通过 ai 自动生成 ts 模块，无需手动更改，增加开发效率
- 目前可以通过 jsdoc 人为约束，但是效果不明显
## 五、公共组件及 utils 工具函数
- 目前公共组件抽象较弱，组件耦合 vue2 的模板语法与具体的业务功能，偏向于专一用途组件。开发边界模糊，大部分趋向于在同一个 index.vue 中编写模板，样式, 逻辑。功能不易拓展，到后期也容易形成难以维护的组件。
	- pdf 导出功能，工具函数/组件，包含了 excel处理，pdf 导出，预览等许多复杂功能，是否可以考虑按功能进行拆分
- 目前 utils 内容较多，主要集中在 src/utils,但部分方法直接绑定到了 vue 实例上，容易被混淆和篡改。函数功能较全，但部分函数糅杂业务属性，且没有文档和辅助提示，导致开发使用起来不友好，有一定的使用成本。
	- vue2 在原型上可以装载方法，如果在某一个页面中篡改了一个vue实例上的方法（`$emit`、`$on`、`$nextTick`、`$destroy`等等）很可能会导致引入这个同名方法的组件执行异常，vue3已经移除在原型上暴露的方法
- 公共组件和utils 函数中会存在大量的重复逻辑处理。会有功能性差异不大的组件，例如目前项目中上传相关组件大概就有4种，主要逻辑一致但是就是返回类型还活着前端交互上的细微差别
- 公共组件体积较大，utils 单个函数逻辑过于复杂
	- 主要是功能公共组件包含大量模板语言和样式以及内聚所有业务逻辑导致，很多只有一个 index.vue 文件，查看代码时只能从头开始梳理组件逻辑
	- 单个 utils 函数处理了过多的逻辑
		- 例如：数组差异比较，可以拆分更细粒度的，对象和基础类型数据比较函数，再通过封装数组比较函数引入这个更细粒度的比较函数，从而达到减少函数体积的目的。当然不是说一定要拆分，而是说 对象和基础类型数据比较函数 也是前端常会用到的方法之一
	- 相同功能的比较因为返回值不同，而写了两个函数，其中大部分逻辑也是类似
- 整理的业务开发模板

| 模板     | 存放位置                                    | 使用场景           | 核心边界                 |
| ------ | --------------------------------------- | -------------- | -------------------- |
| 公共组件模板 | `src/components/ComponentName/`         | 跨页面复用          | 不包含页面路由和业务编排         |
| 业务页面模板 | `src/views/<module>/<page>/`            | 查询、表格、表单、弹窗页面  | `index.vue` 只负责状态和编排 |
| 页面私有组件 | `src/views/<module>/<page>/components/` | 页面专属弹窗、抽屉、复杂区域 | 不允许其他页面直接引用          |
- 组件开发
	- 目录范式
	```text
		src/components/BusinessSelect/
		├── index.vue      # Props、状态、computed、事件和生命周期
		├── const.js       # 有稳定常量时创建
		├── helper.js      # 有纯转换逻辑时创建
		└── readme.md      # Props、Events、Slots、ref API 和示例
	```
	- api 设计范式，基于 element ui 组件开发的组件，需要兼容原有组件的能力，不能导致开发后能力收缩，除非是特定用途的组件

| 能力            | 推荐方式             | 禁止方式             |
| ------------- | ---------------- | ---------------- |
| 数据输入          | Props            | 读取父页面状态          |
| 值变更           | `input`、`change` | 修改 prop          |
| 结构扩展          | Slots            | 大量 HTML 配置 props |
| 主动操作          | 精简 ref 方法        | 暴露完整组件实例         |
| Element UI 属性 | $attrs、作用域 Props | 丢弃未声明属性          |
| Element UI 事件 | $listeners 过滤透传  | 重复触发同一事件         |
| 父子通信          | Events           | $parent          |
| 数据转换          | `helper.js`      | 在模板中复杂计算         |
- 业务模板开发
```text
	src/views/order/order-list/
	├── index.vue                  # 状态、API 编排、组件协调
	├── const.js                   # 枚举、默认值、固定 UI 配置
	├── helper.js                  # 查询构建、校验、转换
	├── tableColumns.js            # 表格列工厂和渲染
	└── components/                # 页面专属业务区域
	    ├── index.js
	    ├── readme.md              # 备注完善的情况下，可以不需要
	    └── OrderEditDialog.vue    # 名称尽量表明功能用途
```

- 标准开发流程范式

| 阶段      | 公共组件                        | 业务页面                                         |
| ------- | --------------------------- | -------------------------------------------- |
| 1. 调研   | 检查复用证据和调用方                  | 检查需求、接口、路由和历史逻辑                              |
| 2. 定边界  | 明确 Props、Events、Slots       | 明确页面状态和业务流程                                  |
| 3. 分文件  | `index/const/helper/readme` | `index/const/helper/tableColumns/components` |
| 4. 实现   | 保留 Element UI 原生能力          | 优先复用公共组件和 API                                |
| 5. 异步处理 | 防重复、防竞态、可销毁                 | loading、异常和局部刷新                              |
| 6. 整理   | 清理无用 API 和样式                | 清理 Mock、无用状态和重复逻辑                            |
| 7. 验收   | 检查全部调用方                     | 检查主流程及相关既有功能                                 |
 - 模板使用边界
 
|场景|应采用的模板|
|---|---|
|一个页面独有的编辑弹窗|页面私有组件模板|
|多个页面复用的选择控件|公共组件模板|
|查询列表页面|业务页面完整模板|
|简单详情页|只保留需要的 `index.vue`、`const.js`、`helper.js`|
|没有复杂表格列|不创建 `tableColumns.js`|
|没有稳定常量或纯函数|不创建空的 `const.js`、`helper.js`|
|修改历史页面|只在需求涉及范围应用模板，不整体迁移|

## 六、统一脚手架能力
- 构建完善的开发流程之后基于组件库能力可以基本实现，基础业务模板开发、收敛 utils 工具函数功能、组件支持业务的能力大大提升，此时可以考虑统一脚手架功能
	- 提供基础的开发模板，一般只会提供 src/views 目录
		- 只需要关注实际的业务开发，不需要关注其他事情
		- 脚手架提供自动识别文件并生成项目路由的能力,路由不需要手动构建
		- layout 自动识别宿主环境，开发环境生成菜单，线上环境隐藏菜单
	- 开发依赖自动注入，所有开发所需的依赖在项目初始化时一并安装，甚至可以隐藏pacakge.json 文件
	- 通过配置生成表单或者详情页面，进而可以使用命令生成基础页面
## 七、单元测试能
- 组件库开发必须，业务开发循序引进 
## 八、前端监控
- 异常监控采用 sentry 用插件进行对接，可捕获现场异常，提醒（warning），可通过邮件告知开发人员，可免费使用
- 日志埋点能力开发
	- 关键业务点的关键数据上报
	- 关键操作上报记录
## 九、Monorepo 工程化？
- 多个项目集中管理，依赖可共享，打包产物可分离
- 配置和脚本集中管理，基本只需要一套
## 十、样式相关
- 目前最大的问题是没有样式规范，没有确定的产品界面风格
- css3,css module,less
